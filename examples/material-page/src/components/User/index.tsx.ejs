import React from 'react';

export default function(props) {
  const { name, age } = props;
  return (
    <div>
      <div><%= userTitle %></div>
      <span>{name}</span>
      <span>{age}</span>
    </div>
  );
}